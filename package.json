{
  "private": true,
  "type": "module",
  "scripts": {
    "biome:cmd//": "echo Expect to not format json files.",
    "biome:cmd": "pnpm biome format"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@biomejs/biome": "1.8.3"
  }
}