{
  "name": "frontend",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "biome:cmd//": "echo Expect to only fix noDoubleEquals. Unfortunately formats other things.",
    "biome:cmd": "pnpm biome format",
    "biome:cmd2": "pnpm biome check --write --unsafe --only=lint/suspicious/noDoubleEquals"
  },
  "dependencies": {},
  "devDependencies": {
    "@biomejs/biome": "1.9.3"
  }
}
